# Prediction Market Bot — Environment Variables
# Copy this file to .env and fill in your keys
# The .env file is git-ignored and will never be committed

# ── Server ────────────────────────────────────────────────────────────────────
PREDICTION_PORT=3003

# ── Kalshi API ────────────────────────────────────────────────────────────────
# Bearer token for Kalshi WebSocket authentication (ticker_v2 real-time data)
# Get from: https://kalshi.com/account/api
# Required for: real-time bracket prices via WS
# Paper run works without this key (falls back to REST polling)
KALSHI_API_KEY=

# ── Gemini Predictions API ────────────────────────────────────────────────────
# Required ONLY when switching to live order execution (mode: 'live')
# Get from: https://exchange.gemini.com/settings/api
# During paper run: leave blank — bot reads Gemini public market data without auth
GEMINI_API_KEY=
GEMINI_API_SECRET=

# ── Alerts (optional) ─────────────────────────────────────────────────────────
# Discord webhook URL — set to enable arb alerts + daily P&L summary
# Leave blank to disable (Telegram support coming in a future version)
# DISCORD_WEBHOOK_URL=
